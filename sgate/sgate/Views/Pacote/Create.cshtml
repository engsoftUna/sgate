@model sgate.Models.pacote

@{
    ViewBag.Title = "Pacotes";
}

<h2>Inserir Pacote</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Inserir Pacote</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.pacote1)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.pacote1)
            @Html.ValidationMessageFor(model => model.pacote1)
        </div>

        <p>
            <input type="submit" value="Salvar" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Voltar", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
